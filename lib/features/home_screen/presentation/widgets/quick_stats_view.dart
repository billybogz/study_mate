import 'package:flutter/material.dart';

class QuickStatesView extends StatelessWidget {
  const QuickStatesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('Quick Stats')],
    );
  }
}
